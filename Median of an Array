
class Solution {
    public double findMedian(int[] arr) {
        // Code here.
        Arrays.sort(arr);
        int len=arr.length;
        if(len%2==1)
        return (arr[len/2]);
        else
        return ((arr[len/2])+(arr[len/2-1]))/2.0;
    }
}

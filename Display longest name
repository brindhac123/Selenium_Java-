class Solution {
    public String longest(List<String> arr) {
        //  code here
        int max=0;
        String ls="";
        for(String s:arr){
            if(max<s.length()){
                max=s.length();
                ls=s;
            }
        }
        return ls;
    }
}

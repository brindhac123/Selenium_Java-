class Solution {
    static boolean searchInSorted(int arr[], int k) {
        // Your code herefor()
        int c=0;
        for(int i=0;i<arr.length;i++){
            if(arr[i]==k)
            c+=1;
        }
        if(c>=1)
        return true;
        else
        return false;
    }
}
